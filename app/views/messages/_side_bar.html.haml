.side-bar
  .user-name
    .user-list
      .list-left
        = current_user.name
      .list-right
        = link_to "#", class: "icon-left" do
          = icon('fas', 'edit')
        = link_to edit_user_path(current_user), class: "icon-right" do
          = icon('fas', 'cog')

  .group-list
    .group-table
      .table-name tech camp
      .table-message まだメッセージはありません